import React, { Component } from 'react';
import {
  AppRegistry,
} from 'react-native';

import moneyManager from './components/App';

AppRegistry.registerComponent('moneyManager', () => moneyManager);